class TaskConfig:

    def __init__(
        self,
        vehicles,
        signals,
        max_steps
    ):

        self.vehicles = vehicles
        self.signals = signals
        self.max_steps = max_steps